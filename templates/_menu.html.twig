<div class="color-auth">
{% if app.user %}
    Bonjour {{ app.user.email }} !
    <a class="color-auth" href="{{ path('app_logout') }}"> Se déconnecter</a>
{% else %}
    <a class="color-auth" href="{{ path('app_login') }}"> Se connecter</a>
{% endif %}
</div>